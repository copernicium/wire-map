#pragma once

#include <memory>

namespace wiremap{
    struct ResultBase{
        virtual ~ResultBase() = default;
    };
    struct ParameterBase{
        virtual ~ParameterBase() = default;
    };
}
